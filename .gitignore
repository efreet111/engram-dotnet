/**/.vs/
/**/bin/
/**/obj/
/*.swp
/.vs/
/dist/
config/mcp/generated/
/publish/
*.user

# Archivo interno de instrucciones para agentes AI
intrusctions.md

# Variables de entorno locales (contienen rutas específicas de cada instalación)
docker/.env

# Secrets y connection strings — NUNCA commitear credenciales
*.env.local
.env.*.local
secrets/

# Project identity — DO NOT ignore .engram-id
# This file contains the UUID v5 project fingerprint and MUST be committed
# so all team members share the same project identity.
# If .engram-id is in .gitignore, team members will have different IDs.
# !.engram-id
*.pem
*.key
*.pfx

# Connection string files (PostgreSQL, etc.)
*.connectionstring
pg-connection.txt
**/connection-string.*

# Test results (TRX, coverage)
*.trx
**/TestResults/
coverage/
*.coverage
coverage.cobertura.xml

# IDE / Editor
.idea/
*.suo
*.sln.ide
.cursor/

# SDD / AI agent temporary artifacts (root-level only — sdd/ changes are tracked)
/.atl/
design.md
design_output.md
design_result.md
dist-cli/

# Engram MCP local memory (per-user runtime state, not project source)
/.engram/

# FlowForge rework tickets live in .ai-work/{slug}/, not at repo root
/rework_ticket.md

# ENG-446 (backlog): Untracked files referenced in BACKLOG but not yet committed.
# Reason: contain known issues or are pending formal review.
# - sync-test.sh: pending validation
# - ADR-002-sync-mutation-application.md: pending formal ADR review
# - ADR-003-xml-doc-comment-policy.md: pending formal ADR review
scripts/sync-test.sh
docs/architecture/adr/ADR-002-sync-mutation-application.md
docs/architecture/adr/ADR-003-xml-doc-comment-policy.md
.directory

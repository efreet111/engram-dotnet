# HU-051 — Git hooks integration (`engram init`)

**ENG:** ENG-481  
**Tipo:** Feature  
**Prioridad:** P1  
**Esfuerzo:** S-M (1 día)  
**Estado:** Idea  
**Origen:** ← feature ideas 2026-08 (ENGRAM-IDEA-005)

---

## Problema que resuelve

Los desarrolladores piensan en commits. El momento en que escriben un commit message es cuando están más propensos a expresar una decisión arquitectónica, un aprendizaje, o un "remember this". Hoy, ese momento pasa y el insight se pierde.

**Sin integración con git, engram es un tool separado del workflow natural.**

---

## Propuesta de solución

`engram init` en un git repo:

1. **Detecta** si estamos en un git repo (buscar `.git/`)
2. **Crea** 2-3 hooks en `.git/hooks/`:
   - `post-commit`: captura commit message + changed files como memoria
   - `prepare-commit-msg` (opcional): prompt interactivo "¿Guardar commit como memoria? [Y/n/edit]"
3. **Los hooks llaman** a `engram save` con los datos del commit

### Comportamiento de los hooks

#### `post-commit` hook
```bash
#!/bin/sh
# Auto-generated by `engram init`
COMMIT_MSG=$(git log -1 --pretty=%B)
CHANGED_FILES=$(git diff-tree --no-commit-id --name-only -r HEAD | tr '\n' ', ')

engram save \
  --title "commit: $COMMIT_MSG" \
  --content "Changed files: $CHANGED_FILES\n\nFull message:\n$COMMIT_MSG" \
  --type commit \
  --project "$(engram project id --quiet)"
```

#### `prepare-commit-msg` hook (opcional, interactivo)
```bash
#!/bin/sh
# Auto-generated by `engram init`
COMMIT_MSG_FILE=$1
COMMIT_SOURCE=$2

if [ -z "$COMMIT_SOURCE" ] || [ "$COMMIT_SOURCE" = "message" ]; then
  echo ""
  read -p "¿Guardar este commit como memoria en engram? [Y/n] " -n 1 -r
  echo
  if [[ $REPLY =~ ^[Yy]$ ]] || [[ -z $REPLY ]]; then
    # Mark for post-commit to capture
    echo "engram-capture=true" >> .git/engram-commit-marker
  fi
fi
```

---

## Criterios de aceptación

- [ ] `engram init` en un git repo:
  - [ ] Detecta `.git/` y crea hooks en `.git/hooks/`
  - [ ] Crea `post-commit` hook que captura cada commit
  - [ ] Crea `prepare-commit-msg` hook (opcional, con flag `--interactive`)
  - [ ] Output confirma:
    ```
    ✓ Git hooks installed:
      - post-commit (auto-capture every commit)
      - prepare-commit-msg (interactive prompt) [optional]
    
    Disable with: rm .git/hooks/post-commit .git/hooks/prepare-commit-msg
    ```
- [ ] Si ya existen hooks:
  - [ ] Backup a `.git/hooks/post-commit.engram-backup`
  - [ ] Warning: "Existing hooks backed up to *.engram-backup"
- [ ] Si NO es git repo:
  - [ ] Error claro: "Not a git repository. Run `git init` first."
- [ ] Hooks funcionan:
  - [ ] Cada commit genera una memoria con `type=commit`
  - [ ] Memoria incluye: commit message, changed files, project ID
  - [ ] Memoria es searcheable: `engram search "commit: fix auth"`
- [ ] Tests:
  - [ ] `engram init` crea hooks correctamente
  - [ ] `post-commit` hook captura commit
  - [ ] Backup de hooks existentes
  - [ ] Error si no es git repo
- [ ] Documentación:
  - [ ] `docs/01-QUICK-START.md` — sección "Auto-capture with git hooks"
  - [ ] `README.md` — mención de `engram init`

---

## Implementación técnica

### Dónde tocar código

1. **`src/Engram.Cli/Program.cs`**:
   - Nuevo comando `init`
   - Lógica de detección de git repo
   - Generación de hook scripts
   - Backup de hooks existentes

2. **Templates de hooks**:
   - `src/Engram.Cli/Templates/post-commit.sh`
   - `src/Engram.Cli/Templates/prepare-commit-msg.sh`
   - O embebidos como string constants en `Program.cs`

3. **Tests**:
   - `tests/Engram.Cli.Tests/GitInitTests.cs` — 5-7 tests

### Edge cases a manejar

- ¿Qué pasa si `engram` no está en PATH?
  - Hook usa ruta absoluta: `$(which engram)` al momento de `init`
  - O usa `engram` asumiendo que está en PATH (más simple, documentar requisito)

- ¿Qué pasa si el repo ya tiene hooks?
  - Backup a `*.engram-backup`
  - Warning al usuario
  - NO intentar merge (muy complejo)

- ¿Qué pasa en Windows con Git Bash?
  - Hooks son shell scripts (funcionan en Git Bash)
  - Documentar: requiere Git Bash o WSL

- ¿Qué pasa si el usuario quiere desactivar?
  - `rm .git/hooks/post-commit .git/hooks/prepare-commit-msg`
  - O futuro: `engram init --uninstall`

---

## Fuera de alcance

- ❌ `pre-push` hook (flag unsynced memories) — future enhancement
- ❌ PR description extraction (GitHub integration) — future enhancement
- ❌ `git tag` hook (mark memories as `release`) — future enhancement
- ❌ `engram init --uninstall` — manual removal por ahora
- ❌ Merge de hooks existentes — backup simple

---

## Cómo probarlo

```bash
# 1. Crear repo de prueba
mkdir /tmp/test-engram-init
cd /tmp/test-engram-init
git init

# 2. Build engram
dotnet build -c Release

# 3. Inicializar hooks
/mnt/86FC44B0FC449BF5/Proyectos/Desarrollo\ Personal/engram-dotnet/src/Engram.Cli/bin/Release/net10.0/engram init
# → ✓ Git hooks installed

# 4. Verificar hooks
ls -la .git/hooks/
cat .git/hooks/post-commit

# 5. Hacer un commit
echo "test" > test.txt
git add test.txt
git commit -m "test: add test file"

# 6. Verificar que se capturó
/mnt/86FC44B0FC449BF5/Proyectos/Desarrollo\ Personal/engram-dotnet/src/Engram.Cli/bin/Release/net10.0/engram search "commit: test"
# → debe mostrar la memoria del commit

# 7. Tests
dotnet test tests/Engram.Cli.Tests/ --filter "GitInit"
```

---

## Métricas de éxito

- **Adopción**: 50%+ de desarrolladores del equipo usan los hooks (el resto puede desactivar)
- **Memorias capturadas**: cada commit genera 1 memoria searcheable
- **Time-to-setup**: <1 minuto (`engram init` + commit de prueba)

---

## Dependencias

- ✅ Ninguna — se puede implementar inmediatamente
- 🔗 Relacionado: ENG-480 (quick-capture), ENG-482 (stats mejorado)

---

## Referencias

- [ENGRAM-IDEA-005](/mnt/86FC44B0FC449BF5/Proyectos/Desarrollo/engram-feature-ideas.md#engram-idea-005--git-hooks-integration-engram-init) — idea original
- [ENG-481 en BACKLOG](../../BACKLOG.md#eng-481)
